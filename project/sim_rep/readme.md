1. Run the 100 simulations

open a terminal in this folder and run the following commands

````
mkdir ../../results/sim
chmod +x cmd_sim.sh
./cmd_sim.sh
````

2. Run `sim_summary.R` for the summary of the simulation studies 
